#!/bin/bash

# Define variables
SCRIPT="./sbin/php-cs-fixer.sh"
FILES=$(git diff --cached --name-only --diff-filter=ACM -- '*.php')

# Run PHP-CS-Fixer
if [[ -n "$FILES" ]]; then
    ${SCRIPT} ${FILES}
    git add ${FILES}
fi
